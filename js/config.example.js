// ============================================================================
// TEMPLATE CONFIG - this file IS committed to GitHub, but has no real
// secrets in it.
// ============================================================================
//
// Setup: copy this file to config.js (same folder) and fill in your own
// three values below. config.js is listed in .gitignore, so `git status`
// will never even offer to add it - it stays on your machine (and wherever
// you manually deploy it) only.
//
//   cp js/config.example.js js/config.js
//
// See the "API keys & GitHub" section of README.md for where each of these
// three values comes from, and for what actually keeps the API key safe
// (hint: it isn't the .gitignore - see that section for why).

export const GOOGLE_SHEETS_API_KEY = 'YOUR_GOOGLE_SHEETS_API_KEY_HERE';
export const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE';
export const GOOGLE_CLIENT_ID = 'YOUR_OAUTH_CLIENT_ID_HERE.apps.googleusercontent.com';
